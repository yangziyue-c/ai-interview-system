# Web 框架
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0

# 数据库（SQLite 默认开发 + MySQL 正式环境）
sqlalchemy[asyncio]>=2.0.30,<2.1
aiosqlite>=0.20.0
aiomysql>=0.2.0

# 缓存
redis>=5.0.0

# 配置与校验
pydantic>=2.8.0
pydantic-settings>=2.4.0

# 鉴权
PyJWT>=2.8.0
bcrypt>=4.1.0

# P2/P3 适配器 HTTP 调用
httpx>=0.27.0

# P3 评估服务（backend/evaluator 独立 Flask 进程，端口 8002）
flask>=3.0.0
flask-cors>=4.0.0
requests>=2.31.0

# 音频上传
python-multipart>=0.0.9

# 题库导入（scripts/import_question_bank.py 读取 题库/*.xlsx）
openpyxl>=3.1.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
