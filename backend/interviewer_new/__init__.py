"""面试官算法（V5 结构化数据源版）

本目录是 1 号（后端主程）配合 V5 知识库换代重构的题库策略，与
`backend/interviewer/`（V4 xlsx 数据源的旧版，已冻结留档）平级：

- `question_bank.py`：题库策略核心（开场 / 三级追问 / 换新 / 收尾 + 锚点层级推断）
- 主后端通过 `app/adapters/ai_interviewer.py` 调用本目录，题库策略是最高优先级数据源

与旧版的关键差异（V5 数据把三级追问拆成了独立字段，详见 `question_bank.py`
模块 docstring 与本目录 README.md）：
- 追问素材直接读结构化列，废弃了 119 行文本解析层；
- L1 触发由关键词匹配改为语义分流（命中率约 66% → 100%）；
- 收尾题池由「收尾交流阶段」改为「行为素质题」题型。

后续算法迭代由 2 号（AI 专项）在本目录进行。
"""
