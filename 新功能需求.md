# 给团队的四项功能建议

## 建议一：加入转发功能（图片 + 链接两种形式）

### 1.1 导出图片

**建议内容**  
在报告页底部增加「分享图片」按钮，把报告页内容生成为 PNG 图片，用户可保存或发送给他人。

**对应队友与分工**

- **前端（4号）负责：**
  - 在报告页加「分享图片」按钮
  - 用 `html2canvas` 把报告区域截图为 PNG，触发下载
  - 兼容桌面端与移动端（移动端可弹窗显示图片，让用户长按保存）

- **后端（1号）负责：**
  - 无需后端支持，纯前端实现

**优先级**：🟡 中等（赛题未要求，属于加分项）

**注意**：分享内容含个人信息，建议加提示「分享内容包含个人信息，请注意隐私」。


### 1.2 生成分享链接

**建议内容**  
在报告页底部增加「生成分享链接」按钮，生成一个可访问的链接，对方打开后能看到这份报告。

**对应队友与分工**

- **前端（4号）负责：**
  - 点击后调后端接口，拿到分享码，复制链接到剪贴板
  - 新增 `/share/:code` 只读页面，展示报告内容（无需登录）

- **后端（1号）负责：**
  - 新增接口 `POST /reports/{interview_id}/share`：
    - 入参：报告 ID（即 `interview_id`）
    - 逻辑：生成唯一分享码（如 UUID），存入数据库，设置有效期（建议 7 天）
    - 返回：`{ share_code, share_url, expires_at }`
  - 新增接口 `GET /share/{code}`：
    - 无需登录
    - 返回：报告数据（结构与 `GET /reports/{interview_id}` 一致，字段包含 `position`、`total_score`、`tech_score`、`logic_score`、`expression_score`、`adaptability_score`、`match_score`、`summary`、`strengths`、`weaknesses`、`suggestions`）
  - 新增数据表 `report_shares`：
    - 字段：`id, report_id, share_code, created_at, expires_at, view_count`

**优先级**：🟡 中等（赛题未要求，属于加分项）

**注意**：分享内容含个人信息，建议加提示「分享内容包含个人信息，请注意隐私」。


## 建议二：加入「回顾面试问答历史」功能

**建议内容**  
在报告页增加一个「查看问答记录」入口，点击后可以看到本次面试的**完整问答对话**（AI 问什么、用户答什么）。

**对应队友与分工**

- **后端（1号）负责：**
  - 最新文档已表明 `GET /interviews/{interview_id}` 返回完整问答记录（用于恢复会话），字段名待确认，建议统一为 `qa_list: [{ question, answer, round, created_at }]`
  - 确保每轮问答都存入了数据库（目前可能只存了最终评估结果，需要补存中间过程）

- **前端（4号）负责：**
  - 报告页加「查看问答记录」按钮
  - 点击后弹窗或跳转新页面，展示问答对列表
  - 用聊天气泡样式展示（AI 左、用户右），和面试对话室视觉一致

**优先级**：🟠 中高（赛题隐含要求「多轮对话」需要可回顾）

**设计建议：**
- 用抽屉式弹窗（从右侧滑出），而不是跳新页面，用户不用离开报告页
- 可以复用面试对话室的聊天气泡样式
- 加一个「复制全文」按钮，方便用户保存


## 建议三：增加「题库中心」板块

**建议内容**  
底部 Tab 栏从 2 个增加为 3 个：**模拟面试 / 题库中心 / 个人中心**。  
「题库中心」用于浏览和搜索面试题目，支持按岗位、题型、难度筛选。

**对应队友与分工**

- **5号（知识库）负责：**
  - 整理现有 Excel 题库，确认字段结构
  - 提供分类标签：岗位（`backend / frontend / test_engineer / algorithm / system_design`）、题型（技术/项目/场景/行为）、难度（easy/medium/hard）
  - 把数据导入后端数据库或提供 JSON 给后端

- **后端（1号）负责：**
  - 新增接口 `GET /questions`：
    - 入参：`position`、`type`、`difficulty`、`keyword`、`page`、`page_size`
    - 返回：题目列表（含题目内容、题型、难度、关键词、参考答案）
  - 新增接口 `GET /questions/{id}`：
    - 返回：题目详情

- **前端（4号）负责：**
  - 底部 Tab 加第三个入口
  - 题库中心页面：
    - 顶部：筛选栏（岗位 / 题型 / 难度）
    - 搜索框
    - 题目列表（卡片形式，点击展开答案）
    - 分页或无限滚动
  - 和岗位大厅视觉统一

**优先级**：🟠 中高（赛题要求「岗位化题库」，展示题库能体现题库规模）

**风险提醒：**
- 题库可能很大（5000+ 题），需要分页
- 如果时间不够，可以先做**只读浏览**，不做搜索和筛选


## 建议四：个人中心按岗位拆分统计

**建议内容**  
个人中心的面试历史，按岗位分开展示；成长曲线和分数对比也按岗位拆分。

**问题背景**  
目前所有岗位的记录混在一起，成长曲线也是混合的。但 **Java 后端和 Web 前端的评估维度不同**，混在一起看没有意义。  
用户想看的是「我 Java 后端练了 5 次，进步曲线如何」。

**对应队友与分工**

- **后端（1号）负责：**
  - `GET /interviews` 增加可选参数 `?position=backend`
    - 不传：返回全部
    - 传了：只返回该岗位的记录
  - `GET /reports/growth` 增加可选参数 `?position=backend`
    - 返回该岗位的成长曲线数据

- **前端（4号）负责：**
  - 个人中心历史列表上方加 Tab：「全部 / Java后端 / Web前端 / 测试开发 / 算法 / 系统设计」
  - 点击 Tab 筛选列表
  - 成长曲线只显示当前筛选岗位的数据
  - 统计卡片（总次数、平均分、最高分）也按当前筛选计算

**优先级**：🔴 高（属于产品完善的核心功能，答辩时体现产品思维）

**短期方案（联调前）：**
- 前端用 localStorage 里的 `position` 字段做本地筛选（注意：最新文档中历史列表字段为 `position`，不是 `jobId`）
- 等后端接口好了再切换到后端筛选


## 建议五：用户可以更改自己的信息

**建议内容**  
在个人中心增加「编辑资料」入口，允许用户修改昵称、学号、目标岗位。

**对应队友与分工**

- **后端（1号）负责：**
  - 新增接口 `PUT /auth/me`（或 `PATCH /auth/me`）：
    - 入参：`{ nickname?, student_id?, target_position? }`
    - 返回：更新后的 `user` 对象（含 `id, username, nickname, student_id, target_position`）
  - 确保 `GET /auth/me` 返回的 `user` 对象包含以上字段

- **前端（4号）负责：**
  - 个人中心弹窗中加「编辑资料」按钮
  - 点击后弹出编辑表单（昵称、学号、目标岗位）
  - 提交后调接口，成功后更新 localStorage 里的 `userInfo`，并刷新页面顶部/侧边栏显示
  - 目标岗位选项从 `GET /positions` 接口获取（5个岗位）

**优先级**：🟠 中高（属于基础用户体验，答辩时体现产品完整度）

**注意**：
- 修改「目标岗位」时需确认是否影响历史记录的统计（历史记录保留原岗位，新面试使用新岗位）
- 如果用户改昵称，头像首字母也要同步更新


## 建议六：用户可以更改自己的头像

**建议内容**  
在个人中心增加「更换头像」功能，用户可上传图片作为头像。

**对应队友与分工**

- **后端（1号）负责：**
  - 用户表新增字段 `avatar_url`（可空）
  - 新增接口 `POST /uploads/avatar`：
    - 入参：`multipart/form-data`，字段 `file`
    - 逻辑：保存图片，返回 `{ url: "/uploads/avatars/xxx.png" }`
  - `GET /auth/me` 返回的 `user` 对象包含 `avatar_url`
  - 更新用户信息接口支持传入 `avatar_url`

- **前端（4号）负责：**
  - 个人中心用户卡片加「更换头像」按钮
  - 点击后选择本地图片，用 `FileReader` 预览
  - 确认后上传到 `/uploads/avatar`，拿到 `url`
  - 调更新用户信息接口，把 `avatar_url` 存入用户资料
  - 更新 localStorage 的 `userInfo`，并刷新所有显示头像的地方（顶部导航、侧边栏、个人中心卡片）
  - 如果没有头像，仍用昵称首字母占位

**优先级**：🟡 中等（加分项，时间充裕再做）

**注意**：
- 限制上传图片大小（建议 ≤2MB）和格式（jpg/png）
- 头像裁剪可选做（不做也行，直接展示原图）
- 移动端需要处理图片选择（`<input type="file" accept="image/*">`）
