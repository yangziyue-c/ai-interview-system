# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 环境配置（含密钥，不入库）
.env

# 需求文档（内部资料，不入库）
a.md

# 数据库文件
*.db
*.sqlite3
# 数据库备份（重建/迁移前的手工备份，不入库）
*.db.bak*
# SQLite WAL 模式伴随文件（运行时产物）
*.db-shm
*.db-wal

# 上传的音频（不入库，目录保留）
backend/uploads/*
!backend/uploads/.gitkeep

# 前端构建产物（由各成员本地构建，不入库）
frontend/node_modules/
frontend/dist/

# RAG 向量库（674MB，可由 rag/数据/*-rag-v2.jsonl 重建，不入库）
# 目录名必须纯 ASCII：chromadb 打不开含非 ASCII 的绝对路径（详见 rag/代码/README.md）
backend/rag/vector_db/
# RAG 检索中间数据（约 70MB，仅重建向量库时需要；主库 *-v5.json 共 16MB 正常入库）
backend/rag/数据/*-rag-v2.jsonl

# 测试与缓存
.pytest_cache/
.coverage
htmlcov/

# IDE
.idea/
.vscode/
*.swp
.DS_Store
.claude/settings.local.json

# AI 工具本地指令文件（本机工作流用，不入库；锚定根目录，避免误伤子目录同名文件）
/CLAUDE.md
