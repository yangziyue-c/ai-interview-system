# 项目背景：AI模拟面试与能力提升软件

我们是一个5人开发团队，正在参加竞赛。我是P1，负责后端架构、核心API开发、部署运维和内网穿透演示。

## 项目目标
为计算机专业学生打造一个AI模拟面试训练系统，针对**后端开发工程师**和**前端开发工程师**两个岗位，提供：
- 语音/文本多模态面试对话（AI面试官动态追问）
- 面试结束后多维度评估报告（技术、逻辑、表达、岗位匹配度）
- 历史记录与能力成长曲线

## 团队分工（5人）

| 角色 | 职责 | 与你（P1）的接口 |
| :--- | :--- | :--- |
| **P1（我）** | 后端架构、数据库设计、面试状态机、核心API、一键启动脚本、内网穿透配置、部署演示 | - |
| **P2（AI对话）** | 大模型对话、RAG检索、Prompt工程、生成题目与追问 | 我提供`session_id`和对话历史，他返回AI问题文本 |
| **P3（AI评估）** | 语音识别集成、多维度评分、综合报告生成 | 我提供完整问答列表和音频URL，他返回评分和评语 |
| **P4（前端）** | Web端界面、语音录制、聊天交互、报告可视化 | 我提供RESTful API，构建后由后端挂载静态文件 |
| **P5（数据/测试）** | 整理题库/知识库、编写测试用例、全流程回归测试 | 我提供测试环境，他负责验收 |

## 我的职责（P1）—— 你需要帮我生成
1. **技术栈**：Python FastAPI + SQLAlchemy（异步）+ MySQL + Redis + JWT鉴权
2. **数据库设计**：用户表、面试会话表、问答记录表、评估报告表
3. **核心业务**：面试状态机（idle → in_progress → finished，最大6轮追问）
4. **API接口**：注册/登录、开始面试、回答并获取下一题、结束面试、获取报告
5. **适配器占位**：为P2和P3预留接口（返回Mock数据），方便他们后续填充
6. **健壮性**：异常处理、统一错误返回、API超时降级（15秒兜底）
7. **运维部署**：
   - CORS配置为通配符`*`（内网穿透演示时允许所有来源访问）
   - 后端启动时绑定`0.0.0.0`（监听所有网卡，允许外部访问）
   - 生成`start.bat`一键启动脚本（Windows环境双击即可启动后端）
   - 提供内网穿透配置说明（推荐Sakura Frp / NatApp），包括映射端口、获取公网地址的步骤
   - 前端构建后的`dist`静态文件由后端挂载（统一端口，避免跨域）

## 输出要求
生成完整的项目目录结构(你需要完成的主要是backend部分)、所有代码文件、requirements.txt、.env示例、启动说明README.md、start.bat脚本示例。由于是多人协作的项目，你需要创建git仓库，提交到github，并且告诉我怎么给其他人提交的权限。
