# A11 AI 对话层集成 · 团队汇报

> 面向 P1~P5 全体。对接细节见两份专项文档：`REPORT_TO_P5_A11_INTEGRATION.md`（给 P5）、
> `REPORT_TO_P4_A11_ENGINE.md`（给 P4）。用法与排障见 `backend/dialogue_layer/README-集成说明.md`。

## 一句话

P5 交付的 A11 AI 对话层已作为**可选面试引擎**接入主项目：出题、追问、五维评分可以整场委托给它，主后端负责把每轮问答与报告写回自己的数据库，历史、成长曲线、学习计划、报告分享都不受影响。默认关闭，配置一个开关即可启用。

## 一、这次做了什么

主后端现在有两条面试链路，由 `interviews.engine` 标记，**开场时定死、整场只读**：

```
                        ┌─────────────────────────────┐
  演示前端 5273 ───────▶│  主后端 8001                 │
                        │  原链路：题库策略 + P3 评估    │──▶ 8002 评估服务
                        │  引擎链路：适配器 + 镜像落库   │──▶ 8005 AI 对话层
                        └─────────────────────────────┘         （P5 的 A11）
```

| 项 | 原链路（默认） | 引擎链路 |
| :--- | :--- | :--- |
| 出题与追问 | 题库策略（`interviewer_new/`） | 对话层内部（判档融合、复读守卫、换题出路、难度自适应、KG 避重） |
| 评分 | P3 评估服务（8002） | 对话层内部（逐轮 LLM 五维评分） |
| 题量 | 7 轮（1 开场 + 6 追问） | 10 题（3/5/2 阶段） |
| 数据落库 | 直接写主库 | 主后端镜像写主库（形状与原来一致） |

代码位置：对话层在 `backend/dialogue_layer/`（P5 的代码一行未改），主后端新增适配器 `app/adapters/ai_dialogue.py` 与报告换算 `app/core/engine_report.py`。

## 二、怎么用

```bash
# 1. 模型权重（约 2.27GB，只下一次）
cd backend
pip install modelscope
python -c "from modelscope import snapshot_download as d; \
  d('BAAI/bge-reranker-v2-m3', local_dir='.hf_cache/bge-reranker-v2-m3')"

# 2. 配 DeepSeek 密钥：backend/.env 里的 LLM_API_KEY
# 3. 启用引擎：backend/.env 里设 DIALOGUE_ENGINE=a11
# 4. 照常一键启动（对话层由 start.py 自动拉起，端口 8005）
```

不设 `DIALOGUE_ENGINE` 时行为与今天完全一致，所有服务照常启动（8005 也会起来，只是面试不走它）。

## 三、验证结果

| 项 | 结果 |
| :--- | :--- |
| 单元测试 | 164 个用例全绿（原 151 个 + 引擎相关 13 个） |
| 算法仿真回归 | 5 岗位全部达标（Mock 兜底 0 次、收尾题按预期出现），原链路行为未变 |
| 桩模式端到端 | 适配器对接真服务：10 题跑通、追问期间题号不推进、SSE 正确解析 |
| 主后端端到端 | 一场 10 题落库：`qa_records` 恰好 10 行、题面与题库**逐字匹配 0 行失配**、学习计划反查到知识点 |
| 真模型 | `bge-reranker-v2-m3` 加载 11.8 秒，语义打分对照：得分点原文作答 100 分、无关作答 0 分 |
| 兼容性 | `sentence-transformers 6.0.1` + `transformers 5.17.0` 下正常，未降级共享环境 |

## 四、对各成员的影响

| 成员 | 影响 |
| :--- | :--- |
| P2（出题） | 引擎模式下不出题：整场委托对话层，`interviewer_new/` 的题库策略与 RAG 兜底、LLM 直连三条源都不参与；**代码未改**，原链路下不受影响 |
| P3（评估） | 引擎模式下 8002 不被调用（评分由对话层做）；服务本身仍由 start.py 拉起，原链路下不受影响 |
| P4（前端） | **契约未破，零改动**；新增三个加法（`/config.engine`、`growth.engine` 与筛选参数、错误码 50300 需与 500 分开处理） |
| P5（知识库 / 对话层） | 交付包已接入，**代码未改**，三处适配见给 P5 的回执；题库被对话层复用（内容已核对一致的同一份），将来更新 `*-v5.json` 时两条链路同时生效 |
| P1（集成） | 落库映射、报告换算、启动集成、文档 |

## 五、已知边界

- **两种链路的分数不可直接比较**：原链路按题注入校准锚点，对话层逐轮 LLM 打分。成长曲线已带 `engine` 字段并支持按链路筛选，前端暂未接入。
- **对话层未就绪时不回落**：引擎模式下若 8005 不可用，面试接口明确返回 503（50300），不会静默换回原链路。这是刻意的，避免分数口径中途变化而事后无法分辨。
- **对话层的会话在内存里**：TTL 2 小时、容量 200 场；服务重启会丢会话，但主后端已镜像落库，已答内容不丢。
- **RAG 保持关闭**：本机整机内存 15.2 GB，低于对话层文档建议的「开 RAG ≥16 GB」门槛；三件数据已备好，将来改一个环境变量即可开。
- **未验证**：对话层文档里那两条欠账（判档真超时的耗时行为、9 场对照重跑）仍未补，需要 API key 与独占 8005 的时间窗口。

## 六、下一步

1. 配 `backend/.env` 的 `LLM_API_KEY`，跑一场真引擎面试，核对报告与成长曲线。
2. 决定是否把成长曲线的链路筛选接到前端（接口已就位）。
3. 与 P5 约定真 LLM 对照的时间窗口（他那两条欠账）。

## 七、第三版更新（2026-09-25）

P5 交了新版对话层：端点从 6 个扩到 10 个（新增语音转写 `/asr`、专项练习 `/practice`、成长档案 `/growth`、参考答案 `/model_answer`），`/finish` 多了 `digest` 与 `review` 两个顶层键，`core/` 多了 7 个能力模块。本项目已按新版更新。

| 项 | 变化 |
| :--- | :--- |
| 引擎 | 代码与数据全量替换（**仍未改一行**），桩模式冒烟 **2854 项 0 失败** |
| 依赖 | 补装语音转写三件套（`faster-whisper`、`ctranslate2`、`av`）；模型再加 whisper-small 464MB |
| 新接口 | `POST /api/v1/uploads/audio/asr`（语音转写，一次调用给文本 + 地址）、`GET /api/v1/reports/archive`（成长档案） |
| 落库 | `reports.digest`：每场 4~7KB 的档案摘要，原样回传给 `/growth` 换错题本 / 考点地图 / 历史成绩 |
| 演示前端 | 语音流程切到后端转写（松开话筒自动转写，文本可改后再发送） |
| 验证 | 真音频转写实测通过（模型加载 1.6 秒、17 秒音频转写 4.6 秒）；档案链路端到端跑通 |
| 未接 | 专项练习、参考答案、复盘清单三个出口，等前端做界面时一并接 |

对成员的影响与上一版相同：引擎模式下 P2 的出题与 P3 的评估不参与（代码与服务都未改，原链路照旧）；P4 的契约只增不改；P5 的交付包已接入。

两点如实说明：**情感分析未开**（`superb/wav2vec2-base-superb-er` 在魔搭上找不到，`emotion*` 恒为 null，转写与音量指标不受影响）；**语音转写有内存门槛**（引擎要求空闲内存 ≥ `A11_ASR_MIN_FREE_MB`，本项目按实测调到 800MB，内存不足时转写返 503 而不是硬加载到 OOM，演示前建议关掉不必要的程序）。
