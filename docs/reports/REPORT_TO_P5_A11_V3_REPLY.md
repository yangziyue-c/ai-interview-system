# 回复 P5：V3 集成回执（补交）+ 情感模型部署结果

> 对应你的《回复-给1号-集成答复.md》（2026-09-26）。
> 你要的三件事分别在：**§一** V3 集成回执、**§二** 情感模型结果（`emotion_error` 没得给，因为起来了）、
> **§四** `review` 渲染的答复。
> 上一份给你的回执（`REPORT_TO_P5_A11_INTEGRATION.md`）是 2026-09-24 那版、对应第二版，
> 第三节起为第三版内容；这一份是**独立的、只讲第三版**，你单看这份就够。

## 〇、一句话

第三版已全部接入并验证：情感模型权重按你给的方式放好、**哈希逐文件一致**、加载成功；
你指出的三处（`resource_kps` 口径、2854 项的性质、`note` 渲染）我们已落实到文档与转达。
唯一还给不了答复的是 `review` 渲染排期，那是产品决策，正在向 P1 转呈（§四）。

## 一、V3 集成回执

### 已做

| 项 | 结果 |
| :--- | :--- |
| 代码与数据 | `app/`（29 文件）、`smoke_test.py`、`check_env.py`、`dump_prompt.py`、`build_kb_index.py`、`web/`、`数据/学习资源样例.json` 全量替换，**代码一行未改** |
| 桩模式冒烟 | **通过 2854 项，失败 0 项**（开关：`LLM_MOCK=1` / `RERANKER_MOCK=1` / `A11_KG=1` / `A11_RAG=0` / **`A11_ASR=0`**） |
| 依赖 | 补装 `faster-whisper==1.2.1`、`ctranslate2==4.8.2`、`av==18.1.0`；`python-multipart`、`onnxruntime` 环境里已有（后者 1.30.0，比你的 pin 高一个补丁位） |
| 模型 | reranker 2.27GB、whisper-small 464MB 从魔搭下载；情感模型 361MB 用你给的权重 |
| 落库 | 每场 `/finish` 的顶层 `digest` 存进 `reports.digest`（实测一场 7170 字节） |
| 新接口 | `POST /api/v1/uploads/audio/asr`（转写 + 存盘，一次调用给文本与地址）、`GET /api/v1/reports/archive`（digest 原样回传 `/growth`，取四视图） |
| 演示前端 | 语音流程切到后端转写（松开话筒自动转写，文本可改后再发送，同一文件不重复上传） |
| 本项目测试 | 168 个用例全绿（原 164 + 新增 4） |

### 四处如实说明（与上一份一致）

1. **ASR 内存门槛按本机下调**到 800MB（你的默认 1200）。理由你已在答复里认可，不再赘述。
2. **`raw` 保持你的脱敏默认档**（不设 `A11_RAW_DETAIL`）：得分点原文不入我们的库、不出接口。
3. **`/practice`、`/model_answer`、`review` 三个出口未接**：等前端做界面时一并接（见 §四）。
4. **转写质量受模型规模限制**：small + int8，有错字是正常形态，设计上就是让考生确认修改后再发送。

## 二、情感模型部署结果

### 权重校验

按 `SHA256SUMS.txt` 逐文件核对，**5 个文件全部一致**；目录合计 **378,367,709 字节**，
与放置说明里写的数字分毫不差。

### 放置方式

照 reranker 的放法解成平铺目录 `backend/.hf_cache/wav2vec2-base-superb-er/`，
并在启动注入里显式加了 `A11_ASR_EMOTION_MODEL=<绝对路径>`（你强调的那一行）。
`A11_ASR_EMOTION` 与 `A11_ASR_EMOTION_DEVICE` 都用的默认值，未动。

### 加载结果：**起来了，没报错**

`transformers 5.17.0` 下 `AutoFeatureExtractor` / `AutoModelForAudioClassification`
**可以正常加载**，你担心的弃用没有发生。`/health` 四键：

```
emotion_enabled  true
emotion_ready    true      ← 懒加载完成后
emotion_error    ""        ← 空，没有错误原文可给
emotion_model    D:/…/.hf_cache/wav2vec2-base-superb-er
```

一段中文 TTS 音频（Windows SAPI 合成）实测：

```
emotion      hap  (0.545)
emotion_dist {neu: 0.445, hap: 0.545, ang: 0.010, sad: 0.00001}
loudness     0.0622      loudness_cv 0.331
```

**与你的实测口径吻合**：这段中文同样被判 `hap`。我们已按你的要求把口径写进文档与演示说明：
「我们有一个情感分布；中文上我们**不信它的标签**，只用它看波动」，演示时不会说「情感识别很准」。

首次 `/asr` 含 whisper 与情感两个模型的加载，耗时 40.9 秒；模型都在内存后回归数秒级。

## 三、你指出的三处，我们已落实

| 你指出的 | 我们改了什么 |
| :--- | :--- |
| `resource_kps = 25` 是考点数、不是素材覆盖度（实际已按题目 ID 平铺 5012 条） | 团队汇报与集成说明里的表述已改为「素材源按题目 ID 平铺到 5012 条（4021 篇真题范文 + 991 篇标注示范作答）」，不再用 `resource_kps` 当覆盖度 |
| 2854 项是 `A11_ASR=0` **跳过** 18 项，不是断言丢失 | 文档里统一写成「2854 项 0 失败（ASR 关着跑的，比 ASR 开着少 18 项）」，判据仍是「失败 0 项」 |
| `kind="示范作答"` 时 `note` 必须渲染 | 已作为**硬要求**转达前端（写进 `REPORT_TO_P4_A11_ENGINE.md`）：991 篇的正文是照 STAR 骨架编的通用示例，不显示 `note` 会让考生把虚构经历当范文背 |

另外两条顺带已记进我们的排障笔记：`local_files_only=True` 导致「没下到」表现为「直接报坏」；
新版 `huggingface_hub` 走 Xet 后端、镜像站返 401（我们在魔搭那条路上踩过同一个坑）。

## 四、`review` 渲染：**已拍板、也已做完**（2026-09-26）

你建议的最小动作采纳并当场做完了：

- **后端**：`/finish` 的 `review` 落库进 `reports.review`，随 `GET /api/v1/reports/{id}` 返回
  （契约见本项目 `docs/API.md` 的 4.7 节）；原链路场次与引擎关掉该功能时是 `null`，前端据此决定画不画。
- **前端（演示前端）**：报告页新增「考后复盘」卡片，渲染 `headline`、漏点（带 `advice`）、
  下一步（`actions[].text`）、底部口径说明（`caveats`）；`gaps` 为空时不显示那一组。
- **验证**：一场 10 题面试的报告里 `review` 完整（桩模式下全答到，所以 `gaps` 为空，
  正好验了「没有漏点时不显示那一组」这条）。

`/practice` 与 `/model_answer` 的完整接入仍是另一档工作量（要新接口、新落库、前端新页面），
那两件等排期——赛题 4a / 4b 正落在它们身上。

## 五、你认可与定死的事（我们已归档）

- **三处刻意偏离全部认可**：版本不降级、模型走魔搭、题库复用（另约定真值源以
  `backend/rag/数据/*-v5.json` 为准，发现问题先通气、不各自改）。
- **五维权重就此定死**：权威真值源 = 你的 `config.SCORE_WEIGHTS` 与本项目
  `evaluation_weights.py` / 《评估维度.csv》，两边一致即为准；不再等那份 Excel sheet。
- **两条欠账不补测**：9 场对照你已跑完（结论：机制接通、考生可见产出无改善，低于噪声底）；
  判档真超时不可复现，直接引用你给的三行契约（前端读超时 `/chat` ≥540 秒、`/finish` ≥360 秒；
  已归档 `p50 3.1s / p90 7.0s / 最坏 25.2s`；整场最坏约 900 秒）。这三行已转达前端。
- **numpy pin 不是我们的约束**：记下了，将来试新模型不会为它让路。

## 六、还需要你回的吗

暂时没有。若后续我们接了 `/practice` 或 `/model_answer` 并发现契约问题，会带着具体接口与样例来找你。
