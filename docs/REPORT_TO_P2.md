# 致 P2（AI 对话 / RAG 检索）同学的工作汇报

> 来自 P1（后端）。本文档汇总与你对接相关的后端最新变化与题库新格式，
> 请以此为准开展 RAG 检索与题目生成服务开发。

## 一、后端对接接口（不变 + 一处重要变化）

后端调用你的服务（配置 `backend/.env` 的 `AI_INTERVIEWER_URL` 后生效）：

```
POST {你的服务}/generate
```

```json
{
  "position": "backend",        // 岗位 code —— 注意：已从固定两个枚举改为动态下发
  "round": 2,                   // 当前是第几题（1 开场题，2~7 追问）
  "is_follow_up": true,         // 是否为追问
  "history": [                  // 完整对话历史（含本轮之前的问答）
    { "role": "interviewer", "content": "请先做个自我介绍……" },
    { "role": "candidate", "content": "我来自……" }
  ]
}
```

期望返回：`{ "question": "你下一题的题目文本" }`

**重要变化：岗位不再只有 backend / frontend 两个枚举。**
后端已改为岗位表动态维护（预留 5 个岗位位），岗位清单可能调整。你收到的
`position` 永远是岗位 **code**（当前可能值：`backend` / `frontend` / `test_engineer`），
**请按 code 过滤题库，不要写死岗位列表**。约定 15 秒内未返回 / 非 2xx / 未配置 URL 时，
后端自动降级为内置 Mock 题库，保证流程不中断——你的服务可以放心迭代。

## 二、题库新格式（v3，请按此对接 RAG）

题库同学已交付精修版（当前 Java 后端 150 题已就绪，前端/测试岗位陆续补齐），
新格式 10 列：

| 列 | 说明 | 对你的用途 |
| :--- | :--- | :--- |
| 题目编号 | tech_001 / coding_012 / scene_005 / proj_003 / beh_002 | 唯一标识，前缀=大类 |
| 所属岗位 | 中文岗位名（即将补 `position_code` 列，用 code 过滤） | RAG 岗位过滤 |
| 大类 | 技术知识 65 / 编码与算法 25 / 场景与设计 30 / 项目深挖 15 / 行为面试 15 | 检索维度 |
| 题目分类 | 如 Java基础、并发编程、JVM、排障Debug…… | 检索维度 |
| 难度等级 | easy / medium / hard | 选题策略（见下） |
| 面试问题 | 题干 | 检索主文本 |
| 得分点 | `【basic 0.2】……【core 0.3】……【advanced 0.5】……` 带权重 | 可作为 Prompt 上下文 |
| 追问触发条件 | 见下 | **追问生成的核心依据** |
| 参考答案 | 完整范例 | Prompt 参考 |
| 备注 | 如"高频考点""适合面试开场" | 选题参考 |

### 追问触发条件（重点）

135 题为标准格式，多条规则换行分隔：

```
若提到hashCode则追问HashMap中key的hash计算过程
若提到String则追问String为什么设计为不可变
```

15 题（scene_016~030，排障/选型类）为变体格式：

```
若候选人未涉及此点，则追问：如何区分内存泄漏和内存溢出？
```

**建议的追问生成逻辑**：把候选人上一轮答案与触发条件做匹配（关键词/语义），
命中"若提到 X"则问对应追问；均未命中则用"未涉及此点"的追问；都没有时再让
LLM 基于题库知识点自主生成。

## 三、抽题策略约定（开场 vs 追问）

后端流程：**1 道开场题（round=1, is_follow_up=false）+ 6 道追问（round=2~7）**。

- **开场题**：建议从当前岗位 `easy` 难度池中抽取（备注中含"适合面试开场"的优先）；
- **追问**：按第二节的"追问触发条件"驱动，结合对话历史（`history` 字段）生成。

## 四、其他约定

- 题库文件在仓库根目录 `Java后端面试题库_150题_五大类_精修v3.xlsx`（后续岗位同名补充），
  你的 RAG 服务可定期同步仓库获取最新版；
- 后端完整接口约定见 [API.md](API.md) 附录「P2：AI 面试官」；
- 联调时后端 Swagger：http://localhost:8001/docs 。

有需要后端配合的字段或格式调整，随时提出。
